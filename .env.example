# ========================================
# WeightTrack 环境变量配置
# ========================================
#
# 本地开发（SQLite）：
# DATABASE_URL 和 NEXTAUTH_SECRET 在 .env 文件中已配置
# 直接 npm run dev 即可
#
# 生产部署（Vercel + Postgres）：
# 在 Vercel 项目设置中连接 Vercel Postgres，
# POSTGRES_URL 环境变量会自动注入
# 同时需要设置：
#   NEXTAUTH_SECRET - 运行 openssl rand -base64 32 生成
#   NEXTAUTH_URL    - 你的部署域名，如 https://weight-track.vercel.app
#   DEEPSEEK_API_KEY - DeepSeek API 密钥，用于 AI 饮食顾问功能
#
